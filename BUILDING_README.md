If you have the 'was also declared here' error, like 'LibraryManagedAttributes' was also declared here.', run the 
`npm dedupe` command to clear potentially duplication of the same package in the packages tree.

If you
